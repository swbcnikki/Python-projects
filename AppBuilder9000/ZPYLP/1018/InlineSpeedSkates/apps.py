from django.apps import AppConfig


# App.py file is created to allow for inclusion of any application configuration of InlineSpeedSkates app
# Also allows for storage of application metadata
class InlineskatesConfig(AppConfig):
    name = 'InlineSkates'
